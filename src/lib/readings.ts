export async function getPreviousReading(id: string) {
  // Implement the logic to get the previous reading
  // This is just a placeholder, replace with your actual implementation
  return { id, reading: 0 }
}

